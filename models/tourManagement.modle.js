const mongoose = require("mongoose");

const toureSchema = mongoose.Schema({
       
})
  //model
  const toureManagement = mongoose.model('Products', productSchema)

  module.exports=toureManagement;